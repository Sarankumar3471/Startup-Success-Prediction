# Startup-Success-Prediction
ML model predicting startup survival using classification algorithms
git clone https://github.com/Sarankumar3471/startup-success-prediction.git
cd startup-success-prediction

git add .
git commit -m "Add startup success prediction model"
git push origin main
